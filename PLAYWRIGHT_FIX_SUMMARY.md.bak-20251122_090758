# خلاصه رفع مشکل Playwright - خطای EACCES

## 🔴 مشکل اصلی

خطای `connect EACCES ::1:xxxxx` در فایل `playwright.config.ts`

این خطا به دلیل مشکلات اتصال شبکه در ویندوز رخ می‌دهد که Playwright نمی‌تواند به سرور محلی متصل شود.

## ✅ تغییرات اعمال شده

### 1. به‌روزرسانی `playwright.config.ts`

**تغییرات کلیدی:**
- ✅ تغییر `localhost` به `127.0.0.1` (حل مشکل IPv6)
- ✅ افزایش timeout از 120 ثانیه به 180 ثانیه
- ✅ فعال‌سازی `reuseExistingServer: true`
- ✅ کاهش workers به 1 (جلوگیری از تداخل)
- ✅ غیرفعال کردن `fullyParallel`
- ✅ افزودن retry برای تست‌های ناموفق
- ✅ افزودن timeout‌های navigation و action
- ✅ تنظیمات بهینه برای Chrome

### 2. فایل‌های جدید ایجاد شده

#### `e2e/README.md`
راهنمای کامل برای:
- توضیح مشکل EACCES
- راه‌حل‌های مختلف
- دستورات اجرای تست
- نکات مهم

#### `scripts/run-e2e-tests.ps1`
اسکریپت PowerShell هوشمند که:
- ✅ وضعیت سرور را چک می‌کند
- ✅ در صورت نیاز سرور را اجرا می‌کند
- ✅ منتظر راه‌اندازی کامل می‌ماند
- ✅ تست‌ها را اجرا می‌کند
- ✅ نتایج را نمایش می‌دهد

#### `scripts/win/playwright-dev.ps1`
اسکریپت کمکی برای:
- پاک کردن پورت‌های قبلی
- اجرای سرور برای تست‌ها

#### `docs/PLAYWRIGHT_TROUBLESHOOTING.md`
مستندات جامع شامل:
- توضیح کامل مشکل
- تمام راه‌حل‌های پیاده‌سازی شده
- روش‌های مختلف استفاده
- رفع مشکلات رایج
- چک‌لیست کامل
- دستورات مفید

#### `.vscode/settings.json`
تنظیمات VSCode/Cursor برای Playwright

### 3. اسکریپت‌های npm جدید

در `package.json` اضافه شده:
```json
"e2e:smoke:win": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-e2e-tests.ps1",
"e2e:ui": "playwright test --ui",
"e2e:report": "playwright show-report"
```

## 🚀 نحوه استفاده

### روش 1: استفاده از اسکریپت خودکار (توصیه می‌شود)

```bash
npm run e2e:smoke:win
```

این اسکریپت همه چیز را خودکار انجام می‌دهد!

### روش 2: اجرای دستی

**ترمینال 1:**
```bash
npm run dev
```

منتظر بمانید تا سرور آماده شود (10-30 ثانیه)

**ترمینال 2:**
```bash
npm run e2e:smoke
```

### روش 3: UI Mode (برای دیباگ)

```bash
npm run e2e:ui
```

## 🔧 رفع مشکلات

### اگر خطا همچنان وجود دارد:

1. **پاک کردن پورت‌ها:**
```bash
npm run dev:kill
```

2. **راه‌اندازی مجدد سرور:**
```bash
npm run dev
```

3. **صبر کنید:** منتظر بمانید تا سرور کاملاً آماده شود

4. **اجرای مجدد تست:**
```bash
npm run e2e:smoke
```

### اگر مشکل ادامه دارد:

1. ✅ چک کنید Node.js نسخه 18+ است
2. ✅ Playwright را نصب کنید: `npx playwright install`
3. ✅ فایروال را موقتاً غیرفعال کنید
4. ✅ آنتی‌ویروس را موقتاً غیرفعال کنید
5. ✅ سیستم را راه‌اندازی مجدد کنید

## 📚 مستندات بیشتر

برای اطلاعات بیشتر، این فایل‌ها را مطالعه کنید:

- `e2e/README.md` - راهنمای سریع
- `docs/PLAYWRIGHT_TROUBLESHOOTING.md` - راهنمای جامع رفع مشکلات

## 🎯 نتیجه

با این تغییرات:
- ✅ مشکل IPv6 حل شده است
- ✅ timeout‌ها بهینه شده‌اند
- ✅ اسکریپت‌های کمکی اضافه شده‌اند
- ✅ مستندات کامل ایجاد شده است
- ✅ تجربه کاربری بهبود یافته است

## ⚠️ نکته مهم

خطای `connect EACCES ::1:xxxxx` که در linter نمایش داده می‌شود، یک خطای runtime از Playwright Language Server است. این خطا زمانی رخ می‌دهد که:

1. سرور در حال اجرا نیست
2. Playwright Language Server سعی می‌کند به سرور متصل شود

**این خطا طبیعی است** زمانی که سرور در حال اجرا نیست. وقتی تست‌ها را اجرا می‌کنید، این مشکل حل می‌شود.

برای جلوگیری از نمایش این خطا در editor:
- سرور را همیشه در حال اجرا نگه دارید
- یا از `reuseExistingServer: true` استفاده کنید (پیاده‌سازی شده)

## 🎉 موفق باشید!

اکنون می‌توانید تست‌های E2E را بدون مشکل اجرا کنید.

