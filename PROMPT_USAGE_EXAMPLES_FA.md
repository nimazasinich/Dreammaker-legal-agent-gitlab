# 💡 راهنمای استفاده از الگوی پرامپت (Prompt Template Usage Guide)

این مستند شامل مثال‌های قدرتمند برای استفاده بهینه از الگوی پرامپت است. هر مثال نشان می‌دهد که چگونه بخش‌های «🎯 وظیفه شما (Your Task)» و «📋 قالب خروجی (Output Format)» را برای سناریوهای مختلف تکمیل کنید.

---

## مثال ۱: پیاده‌سازی یک فیچر (Feature) جدید

### 🎯 وظیفه شما:
من قصد دارم یک فیچر جدید اضافه کنم که به کاربر اجازه می‌دهد یک «سیگنال» (Signal) را از AISignalsScanner مستقیماً به Dashboard «پین» (Pin) کند.

بر اساس مستندات:
1. تحلیل کنید کدام کامپوننت‌ها باید تغییر کنند.
2. بهترین روش برای مدیریت State این «سیگنال پین‌شده» چیست؟ (آیا به Context جدیدی نیاز است یا باید از Context موجود استفاده کرد؟)
3. کدام API ها (در صورت وجود) باید فراخوانی شوند؟
4. تغییرات کد مورد نیاز برای Dashboard.tsx و AISignalsScanner.tsx را ارائه دهید.

### 📋 قالب خروجی:
- تحلیل گام‌به‌گام نیازمندی‌ها.
- بلوک‌های کد (Code Blocks) برای هر فایل TSX که نیاز به تغییر دارد.

### نمونه پاسخ مورد انتظار:

```markdown
## تحلیل نیازمندی‌ها

### ۱. کامپوننت‌های درگیر:
- `AISignalsScanner.tsx` - افزودن دکمه Pin
- `Dashboard.tsx` - نمایش سیگنال‌های پین‌شده
- `PinnedSignalsContext.tsx` - مدیریت state سیگنال‌ها (جدید)

### ۲. مدیریت State:
پیشنهاد: ایجاد یک Context جدید به نام `PinnedSignalsContext` زیرا...

### ۳. تغییرات کد:

#### AISignalsScanner.tsx
```tsx
// کد اصلاح‌شده
```

#### Dashboard.tsx
```tsx
// کد اصلاح‌شده
```
```

---

## مثال ۲: رفع یک باگ (Bug Fixing)

### 🎯 وظیفه شما:
یک باگ وجود دارد: در ChartingView، کامپوننت BacktestButton فعال است، حتی زمانی که هیچ نمادی (Symbol) توسط کاربر انتخاب نشده است.

بر اساس مستندات:
1. پراپ‌های (Props) کامپوننت BacktestButton و وضعیت ChartingView را تحلیل کنید.
2. منطق اعتبارسنجی (Validation) که جا افتاده است را شناسایی کنید.
3. کد اصلاح‌شده برای ChartingView.tsx را ارائه دهید تا دکمه به درستی غیرفعال (disable) شود.

### 📋 قالب خروجی:
- توضیح کوتاه ریشه باگ.
- کد کامل و اصلاح‌شده برای فایل ChartingView.tsx.

### نمونه پاسخ مورد انتظار:

```markdown
## ریشه باگ
باگ به دلیل عدم بررسی وجود `selectedSymbol` قبل از فعال‌سازی دکمه BacktestButton رخ می‌دهد. در حال حاضر، دکمه همیشه فعال است و منطق اعتبارسنجی وجود ندارد.

## کد اصلاح‌شده

### ChartingView.tsx
```tsx
import React, { useState, useEffect } from 'react';
import { BacktestButton } from './BacktestButton';

interface ChartingViewProps {
  selectedSymbol: string | null;
}

export const ChartingView: React.FC<ChartingViewProps> = ({ selectedSymbol }) => {
  const isBacktestDisabled = !selectedSymbol || selectedSymbol.trim() === '';

  return (
    <div className="charting-view">
      {/* سایر کامپوننت‌ها */}

      <BacktestButton
        disabled={isBacktestDisabled}
        onClick={handleBacktest}
      />

      {isBacktestDisabled && (
        <p className="warning-text">
          لطفاً ابتدا یک نماد انتخاب کنید
        </p>
      )}
    </div>
  );
};
```
```

---

## مثال ۳: توضیح جریان داده (Data Flow Explanation)

### 🎯 وظیفه شما:
جریان داده (Data Flow) برای «نمای معاملات» (Trading View) را به صورت گام‌به‌گام توضیح دهید.

شرح دهید که وقتی کاربر یک سفارش «Market Order» ثبت می‌کند، چه اتفاقی می‌افتد:
1. کدام کامپوننت‌ها درگیر هستند؟
2. کدام Context ها (مانند TradingContext) استفاده می‌شوند؟
3. کدام endpoint های API فراخوانی می‌شوند؟ (مثلاً `/api/orders/market`)
4. داده‌ها چگونه از طریق WebSocket به‌روزرسانی می‌شوند؟

### 📋 قالب خروجی:
یک لیست شماره‌گذاری‌شده که تمام مراحل، از کلیک کاربر تا به‌روزرسانی UI، را شرح می‌دهد.

### نمونه پاسخ مورد انتظار:

```markdown
## جریان داده Market Order

### ۱. کلیک کاربر
- کاربر دکمه "خرید" را در `OrderForm.tsx` کلیک می‌کند
- تابع `handleSubmitOrder()` فراخوانی می‌شود

### ۲. اعتبارسنجی فرم
- کامپوننت `OrderValidator` داده‌های ورودی را بررسی می‌کند
- در صورت معتبر بودن، به مرحله بعد می‌رود

### ۳. ارسال به Context
- `TradingContext.submitMarketOrder()` فراخوانی می‌شود
- State به `submitting: true` تغییر می‌کند

### ۴. فراخوانی API
- Endpoint: `POST /api/orders/market`
- Headers: Authorization Bearer Token
- Body:
```json
{
  "symbol": "BTC/USDT",
  "side": "buy",
  "quantity": 0.1,
  "type": "market"
}
```

### ۵. پردازش Backend
- سرور سفارش را دریافت می‌کند
- اعتبارسنجی موجودی کاربر
- ارسال به صرافی

### ۶. پاسخ API
- Status: 201 Created
- Response شامل `orderId` و `status`

### ۷. به‌روزرسانی WebSocket
- سرور پیام WebSocket ارسال می‌کند
- Event: `order_update`
- Payload شامل وضعیت جدید سفارش

### ۸. به‌روزرسانی UI
- `TradingContext` state را آپدیت می‌کند
- `OrderHistory` کامپوننت مجدداً رندر می‌شود
- نوتیفیکیشن موفقیت نمایش داده می‌شود
```

---

## مثال ۴: نوشتن پلن تست (Test Plan)

### 🎯 وظیفه شما:
یک پلن تست (Test Plan) جامع برای کامپوننت NewsFeed.tsx بنویسید.

### 📋 قالب خروجی:
یک جدول با ستون‌های: «سناریو تست»، «اقدامات لازم»، «نتیجه مورد انتظار». شامل تست‌هایی برای:
- بارگذاری موفقیت‌آمیز اخبار.
- شکست API (بر اساس مستندات، باید وضعیت خالی نشان دهد نه دیتای Mock).
- تست فیلترهای Sentiment (positive, negative, neutral).
- بررسی رندر شدن صحیح Badges های Impact.

### نمونه پاسخ مورد انتظار:

```markdown
## پلن تست NewsFeed.tsx

| سناریو تست | اقدامات لازم | نتیجه مورد انتظار |
|------------|--------------|-------------------|
| **۱. بارگذاری موفقیت‌آمیز** | Mock کردن API با داده‌های معتبر، رندر کامپوننت | نمایش لیست اخبار با عنوان، تاریخ و محتوا |
| **۲. نمایش Loading** | رندر کامپوننت قبل از دریافت پاسخ API | نمایش Spinner یا Skeleton |
| **۳. شکست API** | Mock کردن API با status 500 | نمایش پیام "خطا در بارگذاری" - بدون داده Mock |
| **۴. فیلتر Positive** | کلیک روی دکمه "مثبت"، بررسی لیست | فقط اخبار با `sentiment: "positive"` نمایش داده شوند |
| **۵. فیلتر Negative** | کلیک روی دکمه "منفی"، بررسی لیست | فقط اخبار با `sentiment: "negative"` نمایش داده شوند |
| **۶. فیلتر Neutral** | کلیک روی دکمه "خنثی"، بررسی لیست | فقط اخبار با `sentiment: "neutral"` نمایش داده شوند |
| **۷. Badge High Impact** | رندر خبر با `impact: "high"` | Badge قرمز با متن "تأثیر بالا" |
| **۸. Badge Medium Impact** | رندر خبر با `impact: "medium"` | Badge نارنجی با متن "تأثیر متوسط" |
| **۹. Badge Low Impact** | رندر خبر با `impact: "low"` | Badge سبز با متن "تأثیر کم" |
| **۱۰. لیست خالی** | Mock کردن API با آرایه خالی | نمایش پیام "خبری موجود نیست" |
| **۱۱. Pagination** | Mock کردن 50 خبر، اسکرول به پایین | Load شدن صفحه بعدی |
| **۱۲. Refresh** | کلیک دکمه refresh | فراخوانی مجدد API و نمایش داده‌های جدید |

### نمونه کد تست:

```typescript
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { NewsFeed } from './NewsFeed';

describe('NewsFeed', () => {
  it('should display news on successful load', async () => {
    // Arrange
    const mockNews = [
      { id: 1, title: 'خبر تست', sentiment: 'positive', impact: 'high' }
    ];
    mockApi.getNews.mockResolvedValue(mockNews);

    // Act
    render(<NewsFeed />);

    // Assert
    await waitFor(() => {
      expect(screen.getByText('خبر تست')).toBeInTheDocument();
    });
  });

  it('should show error state on API failure', async () => {
    // Arrange
    mockApi.getNews.mockRejectedValue(new Error('Server Error'));

    // Act
    render(<NewsFeed />);

    // Assert
    await waitFor(() => {
      expect(screen.getByText('خطا در بارگذاری')).toBeInTheDocument();
      expect(screen.queryByText('Mock Data')).not.toBeInTheDocument();
    });
  });
});
```
```

---

## مثال ۵: بررسی امنیت (Security Review)

### 🎯 وظیفه شما:
کد مربوط به احراز هویت (Authentication) را از نظر امنیتی بررسی کنید.

بر اساس مستندات:
1. آیا توکن‌ها به درستی ذخیره می‌شوند؟
2. آیا XSS یا CSRF محافظت وجود دارد؟
3. آیا داده‌های حساس در localStorage ذخیره می‌شوند؟

### 📋 قالب خروجی:
- لیست آسیب‌پذیری‌های شناسایی‌شده با سطح ریسک (High/Medium/Low)
- پیشنهادات اصلاحی با کد نمونه

---

## مثال ۶: بهینه‌سازی عملکرد (Performance Optimization)

### 🎯 وظیفه شما:
کامپوننت DataTable که ۱۰۰۰ ردیف را رندر می‌کند، بسیار کند است.

بر اساس مستندات:
1. دلایل احتمالی کندی را شناسایی کنید
2. راه‌حل‌های بهینه‌سازی پیشنهاد دهید
3. کد بهینه‌شده را ارائه دهید

### 📋 قالب خروجی:
- تحلیل مشکل با metrics
- حداقل ۳ راه‌حل با مزایا و معایب
- کد نهایی بهینه‌شده

---

## نکات مهم برای استفاده بهتر

### ✅ بهترین شیوه‌ها:
1. **دقیق باشید**: نام فایل‌ها و کامپوننت‌های دقیق را ذکر کنید
2. **Context ارائه دهید**: اطلاعات پس‌زمینه کافی بدهید
3. **قالب مشخص کنید**: دقیقاً بگویید خروجی را چگونه می‌خواهید
4. **محدوده تعیین کنید**: مشخص کنید کدام فایل‌ها باید بررسی شوند

### ❌ اشتباهات رایج:
1. درخواست مبهم بدون جزئیات
2. عدم ذکر قالب خروجی مورد انتظار
3. درخواست بیش از حد گسترده بدون محدوده مشخص
4. عدم ارائه context کافی از پروژه

---

## قالب عمومی برای درخواست‌های جدید

```markdown
### 🎯 وظیفه شما:
[توضیح واضح و مختصر از کاری که می‌خواهید انجام شود]

بر اساس مستندات:
1. [سوال یا نیاز اول]
2. [سوال یا نیاز دوم]
3. [سوال یا نیاز سوم]

### 📋 قالب خروجی:
[دقیقاً مشخص کنید خروجی را در چه فرمتی می‌خواهید:]
- جدول / لیست / متن توضیحی
- با کد / بدون کد
- خلاصه / جزئیات کامل
```

---

**نویسنده**: تیم توسعه Dreammaker
**تاریخ**: نوامبر 2025
**نسخه**: 1.0
