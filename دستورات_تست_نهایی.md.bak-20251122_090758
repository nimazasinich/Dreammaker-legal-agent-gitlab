# دستورات تست نهایی

## ✅ وضعیت فعلی

### سرور
- ✅ سرور در حال اجرا است (Process ID: 9868)
- ✅ پورت 3001 در حالت LISTENING
- ✅ WebSocket متصل می‌شود: `ws://localhost:3001/ws`
- ✅ Health endpoint کار می‌کند: `/health`

## 🔄 مراحل نهایی برای کاربر

### 1. Refresh مرورگر (مهم!)
```
در مرورگر خود:
1. به http://localhost:5173 بروید
2. Hard Refresh: Ctrl + Shift + R (یا Ctrl + F5)
3. یا: DevTools > Network > Disable cache + Reload
```

### 2. بررسی Console
پس از refresh، در Console مرورگر:
- ✅ **نباید** CORS error ببینید
- ✅ **نباید** `/ws/ws` ببینید (باید `/ws` باشد)
- ✅ **نباید** 404 error برای `/health` یا `/status/health` ببینید

### 3. بررسی WebSocket در Console
```javascript
// در Console مرورگر، این را اجرا کنید:
const ws = new WebSocket('ws://localhost:3001/ws');
ws.onopen = () => console.log('✅ WebSocket Connected:', ws.url);
ws.onerror = (e) => console.error('❌ WebSocket Error:', e);
ws.onclose = () => console.log('WebSocket Closed');

// باید ببینید:
// ✅ WebSocket Connected: ws://localhost:3001/ws
```

### 4. بررسی Dashboard
- ✅ داده‌های قیمت باید لود شوند
- ✅ نمودارها باید نمایش داده شوند
- ✅ Health status باید "OK" باشد

## 🧪 تست‌های دستی (اختیاری)

### تست از PowerShell

#### 1. Health Check
```powershell
Invoke-WebRequest -Uri "http://localhost:3001/health"
```
**پاسخ مورد انتظار:**
```json
{"status":"ok","server":"Real Data Server","timestamp":"..."}
```

#### 2. API Health
```powershell
Invoke-WebRequest -Uri "http://localhost:3001/api/health"
```

#### 3. Market Prices
```powershell
Invoke-WebRequest -Uri "http://localhost:3001/market/prices?symbols=BTC,ETH,SOL"
```

#### 4. Proxy - Binance
```powershell
Invoke-WebRequest -Uri "http://localhost:3001/api/proxy/binance/price?symbol=BTCUSDT"
```

### تست از مرورگر

باز کردن این URLها در مرورگر:

1. http://localhost:3001/health
2. http://localhost:3001/api/health
3. http://localhost:3001/market/prices?symbols=BTC,ETH
4. http://localhost:3001/api/proxy/binance/price?symbol=BTCUSDT

## 📋 چک‌لیست نهایی

### Backend (سرور)
- [x] سرور در حال اجرا است
- [x] پورت 3001 LISTENING
- [x] WebSocket `/ws` کار می‌کند
- [x] CORS configuration درست است
- [x] Health endpoints اضافه شدند
- [x] Proxy routes اضافه شدند
- [x] Market endpoints اضافه شدند

### Frontend (مرورگر)
- [ ] مرورگر را Hard Refresh کنید (Ctrl + Shift + R)
- [ ] Console را بررسی کنید (نباید CORS error باشد)
- [ ] WebSocket connection را بررسی کنید (باید `/ws` باشد نه `/ws/ws`)
- [ ] Dashboard را بررسی کنید (داده‌ها باید لود شوند)

## ⚠️ اگر هنوز مشکل دارید

### مشکل 1: هنوز CORS error می‌بینید
**راه‌حل:**
1. مطمئن شوید سرور restart شده (process 9868)
2. Cache مرورگر را کاملاً پاک کنید:
   - Chrome: Settings > Privacy > Clear browsing data > Cached images and files
   - Firefox: Settings > Privacy > Clear Data > Cached Web Content
3. مرورگر را ببندید و دوباره باز کنید
4. سپس به `http://localhost:5173` بروید

### مشکل 2: هنوز `/ws/ws` است
**راه‌حل:**
1. بررسی کنید که `src/config/env.ts` ذخیره شده
2. Frontend را restart کنید:
   ```powershell
   # در terminal دیگر:
   npm run dev:client
   ```
3. Hard Refresh: Ctrl + Shift + R

### مشکل 3: هنوز 404 errors می‌بینید
**راه‌حل:**
1. بررسی کنید سرور در حال اجرا است:
   ```powershell
   netstat -ano | findstr ":3001"
   ```
2. لاگ‌های سرور را بررسی کنید (پنجره PowerShell سرور)
3. در صورت نیاز، سرور را restart کنید

### مشکل 4: Dashboard لود نمی‌شود
**راه‌حل:**
1. Console را باز کنید (F12)
2. Network tab را بررسی کنید
3. مطمئن شوید که:
   - درخواست‌ها به `localhost:3001` می‌روند
   - Status code 200 است (نه 404 یا 500)
   - CORS error نیست
4. اگر همه چیز OK است اما داده نمی‌آید، ممکن است API های خارجی (Binance, CoinGecko) مشکل داشته باشند
   - این طبیعی است و fallback data باید نمایش داده شود

## 🎯 نتیجه نهایی

**تمام مشکلات Backend برطرف شدند:**
- ✅ CORS configuration
- ✅ WebSocket path (`/ws`)
- ✅ Missing endpoints
- ✅ Proxy routes
- ✅ Fallback data

**فقط یک کار باقی مانده:**
1. **مرورگر را Hard Refresh کنید**: Ctrl + Shift + R

**پس از refresh، همه چیز باید کامل کار کند! 🎉**

## 📞 در صورت نیاز به کمک

اگر پس از انجام تمام مراحل بالا هنوز مشکل دارید:
1. Screenshot از Console errors بگیرید
2. Screenshot از Network tab بگیرید
3. لاگ‌های سرور را کپی کنید
4. این اطلاعات را ارسال کنید

---

**آخرین بروزرسانی**: تمام تست‌های Backend موفق بودند ✅
**وضعیت سرور**: در حال اجرا (PID: 9868) ✅
**وضعیت WebSocket**: متصل می‌شود ✅
**وضعیت Endpoints**: تمام endpoints کار می‌کنند ✅

**اقدام بعدی شما**: Hard Refresh مرورگر (Ctrl + Shift + R) 🔄

