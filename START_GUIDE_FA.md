# 🚀 راهنمای سریع شروع کار

## ✅ تنظیمات انجام شده

### 1. فایل `.env.local` ایجاد شد ✓

تنظیمات برای استفاده از **داده‌های واقعی** با بهینه‌سازی مصرف API:

```
✓ حالت Online فعال است
✓ توکن Hugging Face ذخیره شد
✓ Auto-refresh غیرفعال (برای صرفه‌جویی)
✓ Cache 5 دقیقه‌ای فعال است
✓ 5 سیمبل اصلی: BTC, ETH, BNB, SOL, XRP
```

### 2. کد بهینه شد ✓

- درخواست‌های API کاهش یافت
- Cache هوشمند اضافه شد
- Error handling بهبود یافت

## 🎯 راه‌اندازی

### گام 1: اجرای برنامه

```bash
npm run dev
```

این دستور هم frontend و هم backend را اجرا می‌کند.

### گام 2: باز کردن مرورگر

برنامه در آدرس‌های زیر در دسترس است:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001/api
- **WebSocket**: ws://localhost:3001/ws

### گام 3: مشاهده داده‌های واقعی

بعد از باز شدن برنامه:

1. ✅ داده‌های قیمت از Binance دریافت می‌شود
2. ✅ اطلاعات ارزها از CoinGecko می‌آید
3. ✅ تحلیل‌های AI با Hugging Face انجام می‌شود

## 📊 ویژگی‌های فعال

### داده‌های Real-Time
- ✅ قیمت‌های لحظه‌ای 5 ارز اصلی
- ✅ نمودارهای OHLCV واقعی
- ✅ حجم معاملات و تغییرات 24 ساعته

### تحلیل‌های هوشمند
- ✅ سیگنال‌های معاملاتی
- ✅ تحلیل احساسات (با Hugging Face)
- ✅ شناسایی الگوها

### مدیریت مصرف API
- ✅ Cache 5 دقیقه‌ای
- ✅ محدودیت 15 درخواست/دقیقه
- ✅ تاخیر 1 ثانیه بین درخواست‌ها
- ✅ Refresh دستی (نه خودکار)

## 🔄 نحوه Refresh داده‌ها

### روش 1: دکمه Refresh در UI
در هر صفحه، دکمه refresh (🔄) را بزنید.

### روش 2: F5 مرورگر
صفحه را refresh کنید (داده‌ها از cache بارگذاری می‌شوند).

### روش 3: فعال کردن Auto-Refresh (اختیاری)

اگر می‌خواهید داده‌ها خودکار refresh شوند:

1. فایل `.env.local` را باز کنید
2. تغییر دهید:
   ```env
   VITE_AUTO_REFRESH=true
   VITE_REFRESH_INTERVAL=600000  # هر 10 دقیقه
   ```
3. سرور را restart کنید

⚠️ **توجه**: این باعث مصرف بیشتر API می‌شود!

## 🛠️ دستورات مفید

```bash
# اجرای کامل (frontend + backend)
npm run dev

# فقط frontend
npm run dev:client

# فقط backend
npm run dev:server

# ساخت برای production
npm run build

# اجرای production
npm start

# تست‌ها
npm test

# بررسی TypeScript
npm run typecheck
```

## 📈 مانیتورینگ

### بررسی درخواست‌های API

1. باز کردن Developer Tools (F12)
2. تب Network
3. فیلتر: `api` یا `fetch`

باید ببینید:
- ✅ درخواست‌ها موفق هستند (Status 200)
- ✅ تعداد درخواست‌ها محدود است
- ✅ زمان پاسخ مناسب است (<2s)

### بررسی Console

لاگ‌های مفید:
```
🔄 Loading all data...
✅ Prices loaded: 5
✅ Real data flow initialized
⏸️ Auto-refresh disabled to save API calls
```

## ⚙️ تنظیمات پیشرفته

### کاهش بیشتر درخواست‌ها

اگر محدودیت API دارید، در `.env.local`:

```env
# فقط 2 سیمبل
# سپس در src/contexts/DataContext.tsx خط 193:
const priceSymbols = ['BTC', 'ETH'];

# Cache بیشتر
VITE_CACHE_TTL=600000  # 10 دقیقه

# تاخیر بیشتر
VITE_REQUEST_DELAY=3000  # 3 ثانیه
```

### افزودن API Keys بیشتر

در `.env.local`:

```env
# CoinMarketCap (اختیاری)
CMC_API_KEY=your_key

# CryptoCompare (اختیاری)
CRYPTOCOMPARE_KEY=your_key

# NewsAPI (اختیاری)
NEWSAPI_KEY=your_key
```

## 🔍 عیب‌یابی

### برنامه باز نمی‌شود

```bash
# بررسی پورت‌ها
netstat -ano | findstr :5173
netstat -ano | findstr :3001

# kill کردن پروسه‌ها
taskkill /PID <process_id> /F

# دوباره اجرا
npm run dev
```

### داده‌ها نمایش داده نمی‌شوند

1. بررسی Console (F12)
2. بررسی Network tab
3. اطمینان از اجرای backend
4. بررسی اتصال اینترنت

### خطای "Rate limit exceeded"

```env
# در .env.local:
VITE_REQUEST_DELAY=5000  # 5 ثانیه
VITE_MAX_REQUESTS_PER_MINUTE=5
```

## 📚 مستندات بیشتر

- `REAL_DATA_SETUP.md` - جزئیات کامل تنظیمات
- `OPTIMIZATION_GUIDE_FA.md` - راهنمای بهینه‌سازی
- `README.md` - مستندات اصلی پروژه

## 🎉 شروع کنید!

همین الان:

```bash
npm run dev
```

سپس مرورگر را باز کنید:
http://localhost:5173

**داده‌های واقعی آماده است!** 🚀

---

💡 **نکته**: برای صرفه‌جویی در API، از دکمه refresh استفاده کنید نه auto-refresh!

